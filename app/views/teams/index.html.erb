<%= render "components/dashboard_window" do %>
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="items-center mb-6">
    <%= render partial: "components/standard_subheading", locals: { message: "Teams"} %>
  </div>

  <div id="teams" class="w-full">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
      <tr>
        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
          Name
        </th>
        <th scope="col" class="relative px-6 py-3">
          <span class="sr-only">Edit</span>
        </th>
      </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
      <% @teams.each do |team| %>
        <tr>
          <td class="px-6 py-4 whitespace-nowrap">
            <a href="<%= team_path team.id %>" class="font-bold"><%= team.name %></a>
          </td>
          <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
            <a href="<%= edit_team_path team.id %>" class="text-indigo-600 hover:text-indigo-900">Edit</a>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
    <div class="inline-block my-6">
      <%= render partial: "components/standard_button", locals: { message: "New team", path: new_team_path} %>
    </div>
  </div>
<% end %>